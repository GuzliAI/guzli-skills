{
  "skills": ["guzli-mcp-core"],
  "query": "Find our existing contacts and tags in Guzli, then show the agent's tool configuration. Do not change anything.",
  "expected_behavior": [
    "Uses `guzli:find_contacts`, `guzli:browse_tags` and `guzli:get_agent_tool_configuration`.",
    "Reads exposed schemas and uses the Guzli tools after the host's OAuth sign-in.",
    "Reports returned facts without creating contacts or changing configuration."
  ]
}
