interface:
  display_name: "Guzli MCP Core"
  short_description: "Contacts, lifecycle, and Guzli MCP setup"
  brand_color: "#111827"
  default_prompt: "Follow the guzli-mcp-core skill against the connected Guzli MCP server."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "guzli"
      description: "Guzli remote MCP (chat, voice, campaigns, contacts, inbox)"
      transport: "streamable_http"
      url: "https://mcp.guzli.com/mcp"
